<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ukuke.gl.sensormind" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_group_work_white_48dp"
        android:label="@string/app_name"
        android:theme="@style/CustomActionBarTheme" >
        <!-- android:theme="@style/AppTheme" -->

        <activity
            android:name="com.ukuke.gl.sensormind.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>

                <!-- This is the launcher activity! -->
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ukuke.gl.sensormind.DeviceCapabilitiesActivity"
            android:label="@string/title_activity_device_capabilities" >
        </activity>
        <activity
            android:name="com.ukuke.gl.sensormind.AddDeviceActivity"
            android:label="@string/title_activity_add_device"
            android:parentActivityName="com.ukuke.gl.sensormind.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ukuke.gl.sensormind.MainActivity" />
        </activity>

        <service
            android:name=".BackgroundServiceManager"
            android:exported="false" />

    </application>


</manifest>
