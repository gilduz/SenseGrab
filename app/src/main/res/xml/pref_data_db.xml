<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <PreferenceCategory
        android:title="@string/pref_header_data_db" >
        <ListPreference
            android:key="dbFrequency"
            android:title="@string/pref_title_db_frequency"
            android:entries="@array/pref_db_frequency_titles"
            android:entryValues="@array/pref_db_frequency_values"
            android:defaultValue="300"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null" />

    </PreferenceCategory>
</PreferenceScreen>
